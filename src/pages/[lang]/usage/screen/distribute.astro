---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
import { getTranslations, supportedLanguages, isValidLanguage } from '../../../../i18n';

export function getStaticPaths() {
  return supportedLanguages.map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
if (!isValidLanguage(lang)) {
  return Astro.redirect('/ja/usage/screen/distribute');
}

const t = await getTranslations(lang);
const basePath = import.meta.env.BASE_URL;

const breadcrumbs = [
  { label: t.navbar?.home || 'Home', href: `${basePath}${lang}/` },
  { label: t.main?.['usage-title'] || 'Usage', href: `${basePath}${lang}/usage/` },
  { label: 'Screen Area', href: `${basePath}${lang}/usage/screen/` },
  { label: t.toolmenu?.screen?.distribute || 'キーフレーム/レイヤーに配分' }
];
---

<DocsLayout
  title={t.toolusage?.screen?.distribute?.title || 'キーフレーム/レイヤーの配分'}
  description={t.toolusage?.screen?.distribute?.description || ''}
  lang={lang}
  t={t}
  breadcrumbs={breadcrumbs}
  menuType="usage"
>
  <div>
    <h3>{t.toolmenu?.screen?.distribute || 'キーフレーム/レイヤーに配分'}</h3>
    <p><iframe loading="lazy" width="560" height="350" src="https://www.youtube.com/embed/NAdBX1oncQU" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></p>
    <p>{t.toolusage?.screen?.distribute?.description || ''}</p>
  </div>
</DocsLayout>
