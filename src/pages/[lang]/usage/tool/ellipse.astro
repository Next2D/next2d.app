---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
import { getTranslations, supportedLanguages, isValidLanguage } from '../../../../i18n';

export function getStaticPaths() {
  return supportedLanguages.map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
if (!isValidLanguage(lang)) {
  return Astro.redirect('/ja/usage/tool/ellipse');
}

const t = await getTranslations(lang);
const basePath = import.meta.env.BASE_URL;

const breadcrumbs = [
  { label: t.navbar?.home || 'Home', href: `${basePath}${lang}/` },
  { label: t.main?.['usage-title'] || 'Usage', href: `${basePath}${lang}/usage/` },
  { label: 'Tool Area', href: `${basePath}${lang}/usage/tool/` },
  { label: t.toolmenu?.tool?.ellipse || '楕円ツール' }
];
---

<DocsLayout
  title={t.toolusage?.tool?.ellipse?.title || '楕円ツール'}
  description={t.toolusage?.tool?.ellipse?.description || ''}
  lang={lang}
  t={t}
  breadcrumbs={breadcrumbs}
  menuType="usage"
>
  <div>
    <h3>{t.toolmenu?.tool?.ellipse || '楕円ツール'}</h3>
    <p><iframe loading="lazy" width="560" height="350" src="https://www.youtube.com/embed/-DMZ8BnumyI" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></p>
    <h4>{t.toolusage?.tool?.ellipse?.description || ''}</h4>
    <p>{t.toolusage?.tool?.ellipse?.text1 || ''}</p>
  </div>
</DocsLayout>
