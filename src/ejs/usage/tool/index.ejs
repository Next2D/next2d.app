<% const path = "../../../"; %>
<%- include("../../_include/_header", {
    "title": "tool-usage.tool.title",
    "description": "tool-usage.tool.description",
    "path": path,
}); %>

<%- include("../_base-header", {
    "path": path
}); %>

<div class="col-md-12 col-lg-9">
    <div>
      <h3>Tool Area</h3>
      <p>${{ tool-usage.tool.description }}$</p>

      <h4><i id="transform-tool"></i>${{ tool-menu.tool.selection }}$</h4>
      <p>${{ tool-usage.tool.transform.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/transform.html">${{ main.read-more }}$</a></p>

      <h4><i id="shape-transform-tool"></i>${{ tool-menu.tool.shape-transform }}$</h4>
      <p>${{ tool-usage.tool.shape-transform.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/shape-transform.html">${{ main.read-more }}$</a></p>

      <h4><i id="text-tool"></i>${{ tool-menu.tool.text }}$</h4>
      <p>${{ tool-usage.tool.text.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/text.html">${{ main.read-more }}$</a></p>

      <h4><i id="rectangle-tool"></i>${{ tool-menu.tool.rectangle }}$</h4>
      <p>${{ tool-usage.tool.rectangle.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/rectangle.html">${{ main.read-more }}$</a></p>

      <h4><i id="ellipse-tool"></i>${{ tool-menu.tool.ellipse }}$</h4>
      <p>${{ tool-usage.tool.ellipse.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/ellipse.html">${{ main.read-more }}$</a></p>

      <h4><i id="rounded-rectangle-tool"></i>${{ tool-menu.tool.rounded-rectangle }}$</h4>
      <p>${{ tool-usage.tool.rounded-rectangle.description }}$</p>
      <p><a href="<%= path %>${{ main.lang }}$/usage/tool/rounded-rectangle.html">${{ main.read-more }}$</a></p>

      <h4><i id="loading-project-data"></i>Loading project data</h4>
      <p>Load data in '.n2d' format.</p>

      <h4><i id="save-project-data"></i>Save project data</h4>
      <p>Outputs the project data of the active tab in 'n2d' format.</p>

      <h4><i id="export-tool"></i>Export</h4>
      <p>Outputs the project data of the active tab in JSON format that can be played by Player.</p>

      <h4><i id="setting-tool"></i>Setting</h4>
      <p>You can set the project language, export settings, etc.</p>

      <h4>- Layers</h4>
      <p>
        <h5>Hidden Layers (Not Including)</h5>
        <p>Export JSON data without including layers that are set to be hidden in the Timeline Area layer.</p>

        <h5>Hidden Layers (Include)</h5>
        <p>Export JSON data including the layer which is set to be hidden in the Timeline Area layer.</p>
      </p>

      <h4>- Output</h4>
      <p>
        <h5>JSON(compression)</h5>
        <p>This is a JSON file with data compressed by zlib. The compression makes the file lighter, but it may cause an overhead in decompression on the Plyer side.</p>

        <h5>JSON(uncompressed)</h5>
        <p>This is an uncompressed JSON file; no decompression overhead will be incurred on the Player side, but the data size will be large.</p>

        <h5>Video(webm)</h5>
        <p>Output in webm format.</p>

        <h5>GIF(Animation)</h5>
        <p>Output in GIF animation format.</p>
      </p>

      <h4>- Language</h4>
      <p>You can change the display language settings.</p>

    </div>
</div>

<%- include("../_base-footer", {
    "path": path
}); %>