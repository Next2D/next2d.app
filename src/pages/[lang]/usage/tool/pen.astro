---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
import { getTranslations, supportedLanguages, isValidLanguage } from '../../../../i18n';

export function getStaticPaths() {
  return supportedLanguages.map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
if (!isValidLanguage(lang)) {
  return Astro.redirect('/ja/usage/tool/pen');
}

const t = await getTranslations(lang);
const basePath = import.meta.env.BASE_URL;

const breadcrumbs = [
  { label: t.navbar?.home || 'Home', href: `${basePath}${lang}/` },
  { label: t.main?.['usage-title'] || 'Usage', href: `${basePath}${lang}/usage/` },
  { label: 'Tool Area', href: `${basePath}${lang}/usage/tool/` },
  { label: t.toolmenu?.tool?.pen || 'ペンツール' }
];
---

<DocsLayout
  title={t.toolusage?.tool?.pen?.title || 'ペンツール'}
  description={t.toolusage?.tool?.pen?.description || ''}
  lang={lang}
  t={t}
  breadcrumbs={breadcrumbs}
  menuType="usage"
>
  <div>
    <h3>{t.toolmenu?.tool?.pen || 'ペンツール'}</h3>
    <p><iframe loading="lazy" width="560" height="350" src="https://www.youtube.com/embed/tcCOohy956c" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></p>
    <p>{t.toolusage?.tool?.pen?.description || ''}</p>
  </div>
</DocsLayout>
